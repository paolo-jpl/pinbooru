// import { useState } from 'react'
// import { NavLink, Link } from "react-router-dom";

export default function Navbar(){
  return(
    <nav className="navbar">
      Logo, My Collection, Explore, Tags, Login
    </nav>
  )
}